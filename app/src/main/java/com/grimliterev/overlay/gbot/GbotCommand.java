package com.grimliterev.overlay.gbot;

public enum GbotCommand {
    DELAY, GOTO, LABEL, IF,
    TAP, SWIPE, LONG_PRESS,
    KILL, JOIN, ACCEPT, TURNIN,
    REST, SKILL, MOVE, BUY, SELL,
    EQUIP, UNEQUIP, STOP, UNKNOWN
}
